# ==============================================================================
# AUTHENTICATION
# ==============================================================================

# HMAC secret for magic link tokens (generate with: openssl rand -base64 32)
TOKEN_PEPPER=your_token_pepper_here

# RSA-2048 private key for signing JWT sessions (generate with: openssl genrsa -out jwt-private.pem 2048)
# Use the entire PEM file contents including -----BEGIN/END PRIVATE KEY-----
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
YOUR_PRIVATE_KEY_HERE
-----END PRIVATE KEY-----"

# RSA public key for verifying JWT sessions (generate with: openssl rsa -in jwt-private.pem -pubout -out jwt-public.pem)
# Use the entire PEM file contents including -----BEGIN/END PUBLIC KEY-----
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
YOUR_PUBLIC_KEY_HERE
-----END PUBLIC KEY-----"

# ==============================================================================
# APPLICATION
# ==============================================================================

# Application base URL (used for generating magic links and CORS validation)
NEXT_PUBLIC_APP_URL=http://localhost:3002

# ==============================================================================
# DATABASE
# ==============================================================================

# Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase service role key (NOT the anon key - required for admin operations)
SUPABASE_KEY=your_service_role_key_here

# ==============================================================================
# EMBEDDING (OPTIONAL)
# ==============================================================================

# Comma-separated list of domains allowed to embed the waitlist widget
# Leave empty in development to allow all origins
# Example: https://marketing.example.com,https://landing.example.com
ALLOWED_EMBED_ORIGINS=

# ==============================================================================
# EMAIL (OPTIONAL)
# ==============================================================================

# SMTP server configuration for sending magic link and confirmation emails
# System works without email but loses notification capabilities

SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your_smtp_password
SMTP_FROM=noreply@yourdomain.com

# ==============================================================================
# DEVELOPMENT ONLY
# ==============================================================================

# Default organization and user IDs for dev session auto-login
# These are used by /api/dev/login to create test sessions
DEFAULT_ORG_ID=1
DEFAULT_ORG_USER_ID=1